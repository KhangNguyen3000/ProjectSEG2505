<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nguye.seg2505app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.MyProfile" />
        <activity android:name=".Activities.RegisterFinal" />
        <activity android:name=".Activities.WelcomePage" />
        <activity android:name=".Activities.ModifyScreen" />
        <activity android:name=".Activities.ModifyCompany" />
        <activity
            android:name=".Activities.RegisterProvider"
            android:exported="true" />
        <activity android:name=".Activities.ServiceManagement" />
        <activity android:name=".Activities.ServiceManagementProvider" />
        <activity android:name=".Activities.AddNewService" />
        <activity android:name=".Activities.DefaultAvail" />
        <activity android:name=".Activities.Schedule" />
        <activity android:name=".Bench.DateTimeTable" />
        <activity android:name=".TestingActivity" />
    </application>

</manifest>